name = "emby-reverse-panel"
main = "index.js"
compatibility_date = "2026-04-16"

# Optional but recommended for this repo's runtime features.
workers_dev = true

# Cloudflare Cron 使用 UTC 时间；这里是每天北京时间 09:00 自动推送 TG 日报。
[triggers]
crons = ["0 1 * * *"]
